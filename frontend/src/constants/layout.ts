/**
 * Shared layout constants to ensure visual consistency across the application.
 */

// Consistent page padding for all dashboard screens
// p-4 (16px) on mobile -> p-6 (24px) md -> p-8 (32px) lg -> p-12 (48px) xl/2xl
export const PAGE_PADDING = "p-4 md:p-6 lg:p-8 xl:p-12";

// Consistent gap ratio for grids to avoid abrupt jumps
// 16px (gap-4) -> 24px (gap-6) -> 32px (gap-8)
export const GRID_GAP = "gap-4 md:gap-6 lg:gap-8";
